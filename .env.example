# Copy this file to `.env` and set a strong secret.
# Generate one with:
#   python3 - <<'PY'
#   import secrets; print(secrets.token_urlsafe(48))
#   PY

SEARXNG_SECRET=CHANGE_ME_STRONG_SECRET
