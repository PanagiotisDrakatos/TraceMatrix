# Orchestrator
GOOGLE_CSE_API_KEY=
GOOGLE_CSE_CX=

# Reacher
REACHER_BASE_URL=http://reacher:8080

# SearXNG
SEARX_BASE=http://searxng:8080
SEARXNG_SECRET_KEY=change_this_to_a_random_string

# OpenSearch
OPENSEARCH_URL=http://opensearch:9200
OPENSEARCH_INITIAL_ADMIN_PASSWORD=change_this_to_a_strong_password

# Redis
REDIS_URL=redis://redis:6379/0

# Application settings
OSINT_INDEX=osint_pages
EMBED_DIM=384

# Social Analyzer (HTTP only)
SOCIAL_ANALYZER_URL=http://social-analyzer:9005

# PhoneInfoga (HTTP only)
PHONEINFOGA_URL=http://phoneinfoga:5000

# --- Holehe / Maigret integration ---
ENABLE_HOLEHE_IN_ORCHESTRATE=true
ENABLE_MAIGRET_IN_ORCHESTRATE=true
OUTBOUND_HTTP_PROXIES=
REQUEST_TIMEOUT_SECONDS=40
RETRY_BACKOFF_SECONDS=2.5

# --- Optimal Orchestrator (fallback/hybrid/media) ---
# Core services
OPENSEARCH_URL=http://opensearch:9200
SEARXNG_URL=http://searxng:8080
SOCIAL_ANALYZER_URL=http://social-analyzer:9005
REACHER_URL=http://reacher:8080

# Orchestrator plan
ORCH_EXPORT_DIR=/data/exports
ORCH_EXPORT_MAX_ROWS=20000
ORCH_TIMEOUT_SECONDS=45
ORCH_CONFIG=/app/config/orchestrator.fallback.yaml
ORCH_DEBUG_MEDIA=0

# Optional explicit OpenSearch creds if not already present
# OPENSEARCH_HOST=opensearch
# OPENSEARCH_PORT=9200
# OPENSEARCH_USER=admin
# OPENSEARCH_PASSWORD=change_this_to_a_strong_password
